    Register a new user
    ---
    tags:
      - Users
    parameters:
      - name: user_and_password
        in: body
        required: true
        requires:
          - email:
          - password:
          - confirm_password:
        properties:
          email:
            type: string
          password:
            type: string
          confirm_password:
            type: string

    responses:
      201:
        description: Ok
      400:
        description: Missing name/email/password or not a JSON
      409:
        description: Email exists