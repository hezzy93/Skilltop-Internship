    Register a new user
    ---
    tags:
      - Users
    parameters:
      - name: user_and_password
        in: body
        required: true
        requires:
          - email:
          - password:
          - name:
        properties:
          name:
            type: string
          email:
            type: string
          password:
            type: string

    responses:
      400:
        description: Missing name/email/password or not a JSON
      201:
        description: Ok (user created)